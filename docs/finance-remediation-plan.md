# 재무회계 개편 계획

## 전제

이카운트/Clobe가 정본 장부이고 분개장은 그쪽에서 import된다. XD NODE는 분개를 만드는 곳이 아니라 **정본 원장의 검증·분석 레이어**다. 운영은 1인 로컬(`LOCAL_ERP_USER_EMAIL` 기반 `SUPER_ADMIN`, `app/chatgpt-auth.ts`).

이 두 전제가 바뀌면(예: 배포 재개, 인원 증가, XD NODE 자체를 정본 장부로 전환) 아래 범위 결정부터 다시 검토해야 한다.

## 범위

**범위 안**
- 대사 엔진 — 원천 ↔ 장부 ↔ 신고 3-way, 보조부 ↔ GL tie-out
- 결손 탐지 — 부가세 미상계, 매출원가 누락, 충당금 미설정을 찾아 경보
- 세무 값 정밀화 — 세액 필드, 과세유형, 원천징수 항목 분해
- 회계 표시 정확성 — 계정분류, 손익 라인, 유동·비유동 구분
- 데이터 신뢰성 — 기준일 단일화, 스냅샷 자체 검증, 출처 추적
- 화면 재편 — 출처 배지, 대사 보드, 경보 피로 해소

**범위 밖**
- 보조부 자동분개(매출원가·매입채무·대손·처분손익 전표 생성) — 정본이 아니므로 만들지 않는다
- 결산분개(집합손익 → 이익잉여금 대체) — 이카운트가 한다
- 부가세 정산분개 — 탐지만 하고 분개는 만들지 않는다
- 간이세액표 자동 계산 — 부양가족 정보가 없다. 외부 산출값을 받아 대사한다
- 이연법인세 — 일시적차이 관리 데이터가 전무하다
- 직무분리(SoD) 강제 — 1인 운영. 대신 단독 승인 사실을 기록으로 남긴다

## 순서와 근거

| 단계 | 내용 | 왜 이 순서인가 |
|---|---|---|
| 0 | 기준 확정 | 기준일이 어긋난 채 대사하면 나온 차이가 진짜인지 기준 차이인지 구분 불가 |
| 1 | 정본 분리 | 대사의 한쪽 변이 오염돼 있으면 tie-out을 만들어도 항상 틀린다 |
| 2 | 대사 축 | 검증 레이어의 본체. 규모가 가장 크다 |
| 3 | 세무 정밀화 | 세무 3-way는 일반 3-way 틀 위에 얹는 특수 케이스 |
| 4 | 화면 재편 | 보여줄 것이 확정된 뒤에 짜야 두 번 안 한다 |

순서와 무관하게 즉시 처리: 자금일보 통화 필터, 기간검증 연도 하드코딩 제거, 감가상각 전표일 — **2026-08-26 완료 (본 세션)**.

## 단계 0 — 기준 확정 (M · 3~5일 + 실무 확인)

지금은 같은 개념이 세 개의 기준일을 갖는다 — 스냅샷 asOf, 세금계산서 인사이트 as-of, 실제 최종 전표일. 이 격차가 매출 예측에서 34.6억 차이를 만든다.

- **세무회계 값**: 판관비 모순 규명(실무 확인) · 부가세 계정 실사(세무대리인) · 급여 원천징수 실태(명세서 원본) · 2025 시산표 부호 이상 정리 · 매입 구성 분해(상품매입/수입/판관비성)
- **데이터 가공·처리**: as-of 상수 통합, 스냅샷에 `dataThrough` 신설, 생성 스크립트에 자체 검증(금액·건수 양방향 대사, 부호 검사, 날짜 범위) 추가, `differenceKrw` 부호 정정, 세금계산서 건수 −78건 원인 규명, 배열별 커버리지 메타
- **기능**: 파생 계산 기준일 통일, 예측 시나리오 축퇴 해소, 스냅샷 품질 게이트
- **UI·UX**: 출처 배지 체계(정본원장/세금계산서/은행/스냅샷/추정), 기준일 바 고정, 수치 클릭 시 출처 팝오버, CSV 헤더에 기준일·출처·품질상태

완료 기준: 화면의 임의 숫자를 짚어 "언제까지의, 어디서 온 값인가"를 즉답할 수 있다.

## 단계 1 — 정본 분리 (L · 5~8일)

`finance_journal_entries`(ERP 지급전표)가 마감잠금·계정마스터·결재를 우회하며 정본 원장에 합산되는 문제를 원장 집계에서 제외하는 방식으로 해결한다.

- **세무회계 값**: 계정분류를 계정 마스터 기준으로 전환(코드 첫자리 휴리스틱은 폴백), 4/5자리 코드 정규화, 손익계산서 라인 세분화(매출/매출원가/판관비/영업외수익/영업외비용/법인세), 재무상태표 유동·비유동 구분, 이월이익잉여금 행 신설
- **데이터 가공·처리**: `finance_journal_entries`를 원장 집계에서 제외, 계정 마스터에 `statement_line`/`liquidity`/`contra_of`/`code_aliases` 추가, 분류 로직 이중화 해소
- **기능**: 회계등식 검증 재설계(YTD 고정 + 분류 검증 편입), DRAFT/OFFICIAL 판정 강화, 지급전표를 "지급 실행 기록"으로 재정의, 마감 SUBMITTED 구간 원장 잠금, 감사 가드레일 테스트를 재무 23개 라우트로 확장
- **UI·UX**: 원장 화면에 "이카운트 import분만" 고정 문구, 손익계산서 표준 양식 재렌더, 지급 실행 기록 탭 분리, "당기순이익" 라벨 정정

완료 기준: 조회 시작일과 무관하게 회계등식 성립, 영업이익 산출, OTHER 버킷 0.

## 단계 2 — 대사 축 (XL · 8~12일, 최대 규모)

- **세무회계 값**: 보조부 ↔ GL tie-out 5종(재고자산·매출채권·매입채무·유형자산 장부가·차입금), 은행계정조정표, 대손충당금 권장액 산출, 매출원가 추정 대사, 유형자산 처분손익 계산, 부가세 미상계 탐지
- **데이터 가공·처리**: `finance_reconciliation_statements` 신설, 보조부별 as-of 잔액 표준화, 미수잔액 산식 단일화, 재고 이동평균 교정, `blockedPeriods()` 일원화
- **기능**: 월마감 통제를 체크리스트에서 잔액 일치 검증으로 전환, 대사 차이 사유 등록(구조적/미확인 분리), 회계기간 개설을 명시적 행위로 분리, 낙관적 잠금 표준화
- **UI·UX**: 대사 현황 보드, 차이 드릴다운, 마감 화면 재편(PASS/FAIL이 아닌 차이 금액 중심)

완료 기준: 월마감 시 tie-out 5종 + 은행조정 + 부가세 탐지가 금액으로 나오고, 미해소 차이가 있으면 마감 차단.

## 단계 3 — 세무 정밀화 (L · 5~8일)

자동 계산 도입이 아니라 수기 입력값을 검증할 수 있는 구조를 만든다.

- **세무회계 값**: 공급가액·세액 분리, 세금코드 과세유형(과세/영세율/면세/불공제/대상외), 불공제 매입세액 원가가산 누락 탐지, 원천징수 항목 분해(사용자부담분 별도), 비과세 한도 상수 + 시행일자 이력, 퇴직소득 분리
- **데이터 가공·처리**: `finance_tax_periods`에 장부 계상액 추가(3-way 완성), 신고 주기 필드, 구조적 차이 입력란, 수정세금계산서 사유코드
- **기능**: 세무 검토 완료 조건을 금액 임계치 기반으로 강화, 활성 세금코드 검증 강화, 부가세 신고 요약표(홈택스 대조용), 매입 세액 대조 경고
- **UI·UX**: 세무 화면 3-way 대사 표, 차이 사유 구조적/미확인 분리, 급여 명세 항목별 공제 표, 퇴직정산 라벨 정정

완료 기준: 부가세 신고 시즌에 홈택스 값과 대조해 차이 사유를 전부 설명 가능. 급여대장에서 원천징수영수증으로 이어지는 항목이 빠짐없이 저장.

## 단계 4 — 화면 재편 (L · 5~8일)

- **데이터 가공·처리**: 정적 스냅샷 파생 화면과 실시간 원장 화면의 데이터 경로 분리 명시, 리스크 모델 재보정, 중앙값 기준선 레벨시프트 보정, 은행거래 분류율 개선
- **기능**: 자금예측 회수확률 연령구간별 차등, 자금일보 7일 예정액 이중계상 해소, 최소운영자금 저장 버그 수정, 경보 임계값 재설정
- **UI·UX**: 24개 화면을 목적별 3군(대사·검증/경영판단/실행기록)으로 재그룹, 스냅샷 배지 상시 노출, 첫 화면을 "오늘 조치할 것" 중심으로, CSV를 승인된 원장 기준으로 전환

완료 기준: 첫 화면에 뜬 항목이 전부 실제 조치 대상. 스냅샷 기반과 원장 기반 숫자가 시각적으로 구분.

## 착수 전 선행 확인 (코드로 풀 수 없음)

1. **2025년 판관비가 실제로 얼마인가** (대표 확인) — 재무 히스토리 함의 30,648,039원 vs 급여대장 인건비 463,676,242원. 매출총이익률·손익 추세·예산 실적 전부의 전제.
2. **부가세 계정 잔액이 정상인가** (세무대리인) — 대급금 4,393,035,450원·예수금 5,013,279,478원 2년 미상계, 미지급세금 계정 없음.
3. **원천징수를 실제로 했는가** (급여 원본) — 287행 중 284행 공제 0원. 단계 3 설계가 이 답에 달려 있음.

## 1인 로컬 운영에 따른 조정

진단에서 HIGH로 잡았으나 로컬 단독 운영 확인 후 등급을 내린 항목. 없애지 않고 기록으로 남기는 형태로 전환.

- 자기결재 차단 강제하지 않음 — 대신 감사기록에 "단독 승인" 플래그
- 재무 결재선 2단계 → 1단계 축소 검토(현재 `FINANCE_ADMIN → SUPER_ADMIN`이 동일인으로 수렴)
- `finance:admin` 권한은 역할 맵에 명시만
- 감사증적은 그대로 유지, 가드레일 테스트는 단계 1에서 확장

## 진행 로그

- **2026-08-26**: 즉시 수정 3건 완료 — 자금일보 `currency='KRW'` 필터([app/api/finance/daily-treasury/route.ts](../app/api/finance/daily-treasury/route.ts)), 기간검증 연도 하드코딩 제거(`close`/`daily-treasury`/`debt`/`expense-control`/`fixed-assets`/`general-ledger`/`management-report`/`project-costing`/`tax` 9개 라우트), 감가상각 전표일을 `-28` 고정에서 실제 월말로([app/api/finance/fixed-assets/route.ts](../app/api/finance/fixed-assets/route.ts)). 빌드·린트 통과, `finance-data`/`workflow-ledgers`/`erp-platform` 테스트 145/146 통과 — 실패 1건은 `tests/finance-data.test.mjs:76`의 하드코딩된 `asOf`/`differenceKrw` 기댓값이 2026-08-26 스냅샷 갱신(커밋 `5565663`)과 어긋난 기존 드리프트로, 이번 변경과 무관.

## 참고 자료

- 진단: [XD NODE 재무회계 진단](https://claude.ai/code/artifact/be365e52-2010-4e1c-87bd-14ec6707c60e)
- 기획안 원본(시각화): [XD NODE 재무회계 개편안](https://claude.ai/code/artifact/a15662c7-d11a-4ede-8c0c-1a5519f55115)

## 진행 로그 (계속)

- **2026-08-26 · 단계 1 착수**: 로컬 D1을 직접 조회해 `finance_posting_lines`/`finance_journal_entries`/`finance_opening_balance_*` 테이블이 아직 하나도 생성되지 않았음을 확인 — 원장 관련 기능이 실사용된 적이 없어 데이터 손실 위험 없이 지금 구조를 바로잡을 수 있는 시점으로 판단하고 진행함.
  - **정본 분리**: `finance_journal_entries`(ERP 지급전표·감가상각 전표)를 총계정원장 집계에서 제외([finance-ledger-snapshot.ts](../app/finance-ledger-snapshot.ts), [general-ledger/route.ts](../app/api/finance/general-ledger/route.ts)). 지급전표 생성·전기 기능 자체는 그대로 유지(계속 "지급 실행 기록"으로 추적됨) — 원장 집계에서만 제외. [finance-general-ledger-plan.md](finance-general-ledger-plan.md) 갱신.
  - **회계등식 YTD 고정**: `buildOperationalFinancialStatements`가 조회기간 movement 대신 YTD 누적(ending balance) 기준 손익으로 등식을 검증하도록 수정 — 조회 시작일이 1월 1일이 아닐 때 항상 허위로 깨지던 문제 해소. 화면에 표시되는 손익계산서(조회기간 기준)는 그대로 유지.
  - **정상잔액 방향 이탈 검사 추가**: `quality.normalBalanceMismatch` — 자산↔부채↔자본, 수익↔비용처럼 등식 부호가 같아 원리상 탐지 못하는 오분류를 별도로 표면화(비차단, 정보 제공용 — 감가상각누계액 등 정당한 차감계정 오탐 방지를 위해 마감 차단에는 아직 연결하지 않음).
  - **계정분류 휴리스틱 교정 + 중복 제거**: `openingAccountCategory` 하나로 통합(`master-data/route.ts`의 중복 `categoryFor` 삭제). 실제 계정체계(4=매출/매출원가 혼재, 9=영업외수익/비용 혼재)에 맞춰 코드 첫자리만으로 4·9번대를 추정하지 않도록 변경, "수익" 키워드 추가(이자수익·배당금수익 등 이전엔 미분류로 빠지던 계정 포착), 5번대를 REVENUE 폴백에서 EXPENSE 폴백으로 이동.
  - **가드레일 테스트 확장**: 재무 22개 변경 라우트 전체가 `authorizeErpRequest`+`writeErpAudit`을 갖추도록 검증하는 테스트 추가(`general-ledger`는 읽기 전용이라 제외). 이 과정에서 `assistant` 라우트가 쓰기를 하면서 감사 로그가 없던 실제 결함을 발견해 `responseWithHistory()`에 `writeErpAudit` 호출 추가로 수정.
  - **라벨 정정**: `page.tsx`의 "당기순이익... 세후 결산 계정" 힌트 — 법인세비용 계정이 차트에 없어 세후라고 단정할 근거가 없으므로 "법인세비용 계정 없음 · 세전·세후 여부 미확인"으로 정정.
  - **검증**: 빌드 통과, `erp-platform` 85/85, `finance-data`+`workflow-ledgers` 62/63(실패 1건은 08-25→08-26 스냅샷 갱신에 따른 기존 드리프트로 이번 변경과 무관, 기존에도 실패 중이었음).
  - **미완료(다음 세션)**: 손익계산서 라인 세분화(매출/매출원가/판관비/영업외수익/영업외비용/법인세), 재무상태표 유동·비유동 구분 + 이월이익잉여금 행, 지급 실행 기록 별도 탭 UI.

- **2026-08-26 · 마감 SUBMITTED 구간 원장 잠금 + 해시 재확인 완료**:
  - `posting-control/route.ts`의 `blockedPeriods()`가 `CLOSED`뿐 아니라 `SUBMITTED` 상태도 차단하도록 수정 — 결재자가 검토 중인 기간에 새 전기가 끼어들어 승인 대상 원장이 바뀌는 것을 방지.
  - `api/approvals/route.ts`의 `FINANCE_CLOSE_RUN` 최종 승인 직전에, 제출 시 동결한 `ledgerHash`를 현재 원장에서 재계산해 대조하는 검증을 추가(`FINANCE_MASTER_CHANGE`의 기존 재검증 패턴과 동일 구조). 계보가 어긋나면 409로 승인을 차단하고 재개방을 안내.
  - **검증**: 빌드·린트 통과, `erp-platform`+`workflow-ledgers`+`finance-data` 147/148(기존 무관 드리프트 1건 유지).
  - **남은 항목**: 손익계산서 라인 세분화, 재무상태표 유동·비유동 구분 + 이월이익잉여금 행, 지급 실행 기록 별도 탭 UI.

- **2026-08-26 · 손익계산서 라인 세분화 + 재무상태표 유동·비유동 구분 완료**:
  - `finance-opening-balance.ts`에 `statementLineFor()`/`liquidityFor()` 신규 — 실제 27개 계정(2025 시산표) 전체에 대입해 **미분류 0건**으로 검증(코드로 재계산 확인).
  - `finance_master_accounts`에 `statement_line`/`liquidity` 컬럼 추가, 시드·수동등록·결재승인(`approval-engine.ts`) 경로 전부에 연결. 관리자가 필요시 덮어쓸 수 있음.
  - `buildOperationalFinancialStatements`에 매출→매출총이익→영업이익→법인세차감전순이익→당기순이익 5단 소계와 유동/비유동 자산·부채 소계 추가. 세분류가 안 된 손익·자산·부채 계정은 `quality.unclassifiedStatementLineAccounts`/`unclassifiedLiquidityAccounts`로 비차단 표면화(추정보다 미분류가 안전하다는 기존 원칙 유지).
  - `general-ledger-workspace.tsx`를 5단 손익 구조 + 유동/비유동 재무상태표로 재렌더. `3799 당기순이익` 같이 이름이 "당기순이익"이면서 2026년 전기 이력이 0인 자본계정에는 "(전기 이월, 2026년 미변동)" 표시를 붙여, 컴퓨티드 YTD 당기손익과의 혼동을 해소.
  - 결산분개(집합손익→이익잉여금 대체)는 여전히 범위 밖 — 이 표시는 라벨링 수준의 해소이며 회계 분개를 만들지 않음.
  - `finance-general-ledger-plan.md`, `finance-operational-statements-plan.md` 갱신.
  - **검증**: 빌드·린트 통과, `erp-platform`+`workflow-ledgers`+`finance-data`+`rendered-html` 153/155(기존 무관 드리프트 2건 유지, 신규 회귀 없음).
  - **남은 항목**: 지급 실행 기록 별도 탭 UI(단계 4로 이관 검토).

- **2026-08-26 · 단계 2(대사 축) 착수 — 매출채권 대사 1건 구현**:
  - 공통 인프라 신설: `app/finance-tie-out.ts`(`finance_tie_out_checks` 테이블 — 기획 초안의 `finance_reconciliation_statements`를 이 이름으로 확정) + `finance-ledger-snapshot.ts`의 `glAccountBalance()`(계정코드 하나의 원장 잔액을 import-only 기준으로 조회).
  - 대사 판정 규칙: 차이가 없거나 "구조적"으로 검토되면 통과, "미확인"이면 금액과 무관하게 마감 차단. 재계산 시 차이 금액이 바뀌지 않으면 기존 검토 기록을 보존하고, 바뀌면 재검토를 요구(초기화).
  - 첫 대사 항목으로 매출채권(`1089 외상매출금`) 구현 — `app/api/finance/tie-out/route.ts`(GET 조회, POST RECOMPUTE/REVIEW). 보조부 잔액 산식은 `receivables/route.ts`가 화면에 쓰는 정의(ACCEPTED/COMPLETED 수금만 차감)와 동일하게 맞춰, 두 화면이 서로 다른 미수잔액을 말하지 않도록 함.
  - `close/route.ts`에 `RECEIVABLES_TIE_OUT` 통제 추가 — 기존 15개 통제와 같은 방식으로 마감 통과 여부에 반영.
  - `receivables-workspace.tsx`에 대사 패널 추가 — 보조부/원장 잔액 비교, 재계산 버튼, 차이 있을 때 사유(구조적/미확인) 입력 폼.
  - 가드레일 테스트에 `tie-out` 라우트 추가 + 전용 테스트 1건.
  - **검증**: 빌드·린트 통과, `erp-platform` 87/87(전건 통과).
  - **남은 항목(2단계)**: 매입채무·재고자산·차입금 tie-out(같은 패턴으로 순차 추가), 은행계정조정표(별도 구조 필요 — 단순 tie-out이 아니라 미기입예금·미결제출금 항목화), 미수잔액 산식 단일화(자금예측이 아직 다른 정의를 씀), 재고 이동평균 교정, `blockedPeriods()` 공용화, 대사 현황 보드 UI(현재는 매출채권에 한해 receivables 화면에만 있음 — 유형이 늘어나면 전용 보드로 승격 필요).

- **2026-08-26 · 매입채무(2519 외상매입금) tie-out 추가**:
  - `computeCheck()`에 PAYABLES 분기 추가. 보조부 잔액 = 취소되지 않았고 `finance_payment_ledger`에 PAID로 확정된 지급이 아직 없는 매입 인보이스 총액. 부채계정이라 `glAccountBalance()`의 `netDebit`(차변−대변) 부호를 뒤집어 대변 기준으로 비교.
  - `close/route.ts`에 `PAYABLES_TIE_OUT` 통제 추가.
  - `purchasing-workspace.tsx`에 동일한 대사 패널 추가 — 이 파일은 이미 `eslint-disable-next-line react-hooks/set-state-in-effect` 관례를 쓰고 있어(receivables와 다른 마운트 패턴) 그 관례를 그대로 따름.
  - 매출채권 때와 달리, 구매 화면에는 원래 "미지급 총액" 같은 집계가 없었으므로 이번에 새로 정의(취소 제외 + PAID 미확정)했다 — 기존 화면과 충돌할 정의가 없어 자유도가 있었음.
  - 가드레일 테스트 1건 추가.
  - **검증**: 빌드·린트 통과, `erp-platform` 88/88(전건 통과), 전체 스위트 155/157(기존 무관 드리프트 2건 유지).
  - **남은 항목(2단계)**: 재고자산·차입금 tie-out, 은행계정조정표, 미수잔액 산식 단일화, 재고 이동평균 교정, `blockedPeriods()` 공용화, 대사 현황 보드 UI.

- **2026-08-26 · 재고자산(1469 상품) tie-out 추가**:
  - `computeCheck()`에 INVENTORY 분기 추가. 보조부 잔액 = 전 품목·전 창고 이동원장 누적(IN−OUT 금액), inventory/route.ts가 화면 상단 "총 재고가치"로 이미 쓰는 것과 동일한 산식을 전체 합계 한 줄로 재계산.
  - `close/route.ts`에 `INVENTORY_TIE_OUT` 통제 추가.
  - `inventory-workspace.tsx`에 동일 패턴 패널 추가. 이 파일도 `eslint-disable-next-line react-hooks/set-state-in-effect` 관례를 씀 — purchasing과 동일하게 처리.
  - 재고 모듈 자체의 알려진 결함(이동평균이 실제로는 입력순 누적평균, 원 단위 반올림 잔류)은 이번 tie-out으로 고치지 않았다 — 이 대사는 "보조부가 원장과 일치하는가"만 확인하며, 보조부 계산 로직 자체의 정확성은 "재고 이동평균 교정" 항목에서 별도로 다룬다.
  - 가드레일 테스트 1건 추가, 기존 PAYABLES 테스트의 배열 리터럴 검증도 3항목으로 갱신.
  - **검증**: 빌드·린트 통과, `erp-platform` 89/89(전건 통과), 전체 스위트 156/158(기존 무관 드리프트 2건 유지).
  - **남은 항목(2단계)**: 차입금 tie-out, 은행계정조정표, 미수잔액 산식 단일화, 재고 이동평균 교정, `blockedPeriods()` 공용화, 대사 현황 보드 UI.

- **2026-08-26 · 미수잔액 산식 단일화 완료**:
  - `forecast/route.ts`의 13주 자금예측이 `payment.status <> 'CANCELLED'`(DRAFT·SUBMITTED 수금까지 차감)를 써서 미수잔액을 과소계상하던 것을 `receivables/route.ts`와 동일한 `payment.status IN ('ACCEPTED','COMPLETED')`(확정 수금만 차감)로 통일. 원래 첫 진단에서 지적된 항목.
  - 교차확인: `sales/route.ts`의 나머지 두 `<> 'CANCELLED'` 사용처는 버그가 아님 — 하나는 `reserved_amount`(예약 중 금액, `collected_amount`와 의도적으로 분리된 별개 개념), 하나는 신규 결제배분 생성 시 초과배분 방지용 잔여한도 계산(과소 배분 허용보다 과다 배분 차단이 맞는 방향)이라 그대로 둠.
  - 기존 테스트의 낡은 assertion(`payment\.status <> 'CANCELLED'` 존재를 요구)을 수정된 조건 검증 + `doesNotMatch`로 교체.
  - **검증**: 빌드·린트 통과, `erp-platform` 89/89(전건 통과), 전체 스위트 156/158(기존 무관 드리프트 2건 유지).
  - **남은 항목(2단계)**: 차입금 tie-out, 은행계정조정표, 재고 이동평균 교정, `blockedPeriods()` 공용화, 대사 현황 보드 UI.

- **2026-08-26 · 재고 이동평균 교정 완료**:
  - `currentStock()`에 선택적 `asOfDate` 매개변수 추가 — 새 출고·조정 거래의 단가·가용재고를 "현재 전체 누적"이 아니라 "그 거래일까지의 잔고"로 계산하도록 수정. 유일한 호출부(신규 이동 생성 로직)에 `movementDate`를 넘겨 적용. 부수 효과로 미래에 들어올 입고 수량을 포함한 착시 가용재고로 출고를 허용하던 문제도 함께 막힘.
  - 잔량을 전량 출고(수량 = 가용재고)하는 경우, 반올림한 단가×수량 대신 남은 재고금액 전체를 그대로 흡수하도록 수정 — 수량은 0인데 금액만 1원 안팎으로 영구히 남던 잔차 문제 해소.
  - 재고 요약 조회(전체 창고·품목 GROUP BY 쿼리)와 재고자산 tie-out의 보조부 계산은 원래도 날짜 필터가 필요 없는 "현재 총계"라 그대로 둠 — 이번 수정은 신규 거래 생성 시점의 단가·가용성 판단에만 영향.
  - 가드레일 테스트 1건 추가.
  - **검증**: 빌드·린트 통과, `erp-platform` 90/90(전건 통과), 전체 스위트 157/159(기존 무관 드리프트 2건 유지).
  - **남은 항목(2단계)**: 차입금 tie-out, 은행계정조정표, `blockedPeriods()` 공용화, 대사 현황 보드 UI.
